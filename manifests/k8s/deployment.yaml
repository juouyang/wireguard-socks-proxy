apiVersion: apps/v1
kind: Deployment
metadata:
  name: wireguard-socks-proxy
  namespace: edge-newpark
  labels:
    app: wireguard-socks-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wireguard-socks-proxy
  template:
    metadata:
      labels:
        app: wireguard-socks-proxy
    spec:
      containers:
      - name: wireguard-socks-proxy
        image: juouyang/wireguard-socks-proxy:1.0.7-arm64
        imagePullPolicy: Always
        env:
        - name: UPTIME_PUSH_URL
          valueFrom:
            secretKeyRef:
              name: uptime-push-url
              key: UPTIME_PUSH_URL
        ports:
        - containerPort: 1080
          name: socks
        securityContext:
          privileged: true
          capabilities:
            add:
              - NET_ADMIN
        volumeMounts:
        - name: wireguard-config
          mountPath: /etc/wireguard
          readOnly: true
        livenessProbe:
          exec:
            command:
              - ping
              - -c
              - "3"
              - 10.1.1.1
          initialDelaySeconds: 30
          timeoutSeconds: 10
          periodSeconds: 30
          failureThreshold: 3
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
      - name: nginx-reverse-proxy
        image: nginx:stable
        env:
        - name: SWITCH_TOKEN
          valueFrom:
            secretKeyRef:
              name: switch-config
              key: SWITCH_TOKEN
        - name: SWITCH_ON_URL
          valueFrom:
            secretKeyRef:
              name: switch-config
              key: SWITCH_ON_URL
        - name: SWITCH_OFF_URL
          valueFrom:
            secretKeyRef:
              name: switch-config
              key: SWITCH_OFF_URL
        command: ["/bin/sh", "-c"]
        args:
          - |
            envsubst '${SWITCH_TOKEN} ${SWITCH_ON_URL} ${SWITCH_OFF_URL}' < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf
            exec nginx -g "daemon off;"
        ports:
        - containerPort: 80
          name: cam1
        volumeMounts:
        # 把 default.conf.template 掛到 Nginx 預設 conf.d 位置
        - name: nginx-default-conf
          mountPath: /etc/nginx/conf.d/default.conf.template
          subPath: default.conf.template
        # 把 index.html 掛到 Nginx 預設 root 位置
        - name: nginx-index-html
          mountPath: /usr/share/nginx/html/index.html
          subPath: index.html
        - name: cam1-data
          mountPath: /var/local/cam1
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
      - name: cam1-ffmpeg
        image: linuxserver/ffmpeg:latest
        env:
          - name: RTSP_URL
            valueFrom:
              secretKeyRef:
                name: cam1-rtsp
                key: RTSP_URL
        command: ["/bin/sh", "-c"]
        args:
          - |
            mkdir -p /var/local/cam1;
            ffmpeg -rtsp_transport tcp -i "$RTSP_URL" \
              -c copy \
              -f hls \
              -hls_time 0.5 \
              -hls_list_size 3 \
              -hls_flags delete_segments+independent_segments \
              /var/local/cam1/stream.m3u8
        volumeMounts:
        - name: cam1-data
          mountPath: /var/local/cam1
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          exec:
            command:
              - /bin/sh
              - -c
              - test -f /var/local/cam1/stream.m3u8
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
      volumes:
      - name: wireguard-config
        secret:
          secretName: wireguard-config

      - name: nginx-default-conf
        configMap:
          name: nginx-reverse-proxy
          items:
          - key: default.conf.template
            path: default.conf.template

      - name: nginx-index-html
        configMap:
          name: nginx-reverse-proxy
          items:
          - key: index.html
            path: index.html

      - name: cam1-data
        emptyDir: {}
